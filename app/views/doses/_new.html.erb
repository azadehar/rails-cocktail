<div class="container">
  <div class="row justify-content-center">
    <h1>Add new doses</h1>
    <%= form_for @dose do |f| %>
    <%= f.text_field :description %>
    <%= f.button :submit, class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
